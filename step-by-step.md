// FILE: 

# Criação do projeto Vite:
. npm create vite@latest shopcart -- --template react
. cd shopcart
. npm install
. npm install react-router-dom

# Verificação:
. npm run dev

# Estrutura de Pastas:
. mkdir -p src/{components,pages,hooks,services,context,styles,utils}
. mkdir -p src/components/{Header,ProductCard,CartItem,CategoryFilter}
. mkdir -p src/pages/{Login,Products,ProductDetails,Checkout}
